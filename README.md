B&B Manutencao - PWA starter

This project is configured with next-pwa. To test locally:

1. npm install
2. npm run dev (service worker disabled in dev)
3. npm run build && npm start (production build - service worker generated)

To deploy on Vercel: push to GitHub and import into Vercel. next-pwa will generate SW at build time.
